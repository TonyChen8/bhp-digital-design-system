title: Input
componentName: Button
introductionText:
  - Provides a simple, standardized input component which enforces the BHP styling. The input will allow the ability to show a tooltip next to the label and more importantly display any validation errors with the BHP styling. Text fields typically reside in forms but can appear in other places, like dialog boxes and search. Text fields validate input, help users fix errors, autocomplete words, and provide suggestions.They are generaly signle-line inputs.
codeContainer:
  - title: Input box
    type: Example
    text: Bootstrap supports all the HTML5 input types text, password, datetime, datetime-local, date, month, time, week, number, email, url, search, tel, and color. Note Inputs will NOT be fully styled if their type is not properly declared! 
    componentName: alert
    props:
    REACT: REACT
    HTML: |
      <span class='default-text'>Default text input<span><br /><input class='defaultTextInput' type="text" name="fname">
    CSS: |
      .defaultTextInput {
        width: 270px;
        height: 30px;
        border-radius: 2px;
        background-color: #ffffff;
        border: solid 1px #dcdcdc;
      }
      
      .default-text {
        font-family: Arial;
        font-size: 11px;
        line-height: 1.27;
        text-align: left;
        color: #333333;
      }

      .defaultTextInput:focus {
        outline: none;
        border: solid 1px #333333;
      }
    ANGULAR: ANGULAR1
  - title: Variations
    type: Example
    text:
    componentName: alert
    props:
    REACT: REACT
    HTML: |
      <span class='default-text'>Default text input</span><br /><input class='defaultTextInput' type="text" name="fname"><br /><br /><br />
      <span class='default-text'>Focussed text input<span><br /><input class='defaultTextInput' type="text" name="fname" autofocus><br /><br /><br />
      <span class='default-text'>Disabled text input<span><br /><input class='defaultTextInput' type="text" name="fname" disabled><br /><br /><br />
    CSS: |
      .defaultTextInput {
        width: 270px;
        height: 30px;
        border-radius: 2px;
        background-color: #ffffff;
        border: solid 1px #dcdcdc;
      }
      
      .default-text {
        font-family: Arial;
        font-size: 11px;
        line-height: 1.27;
        text-align: left;
        color: #333333;
      }

      .defaultTextInput:focus {
        outline: none;
        border: solid 1px #333333;
      }

      .defaultTextInput:disabled {
        background-color: rgba(220, 220, 220, 0.2);
        border: solid 1px #dcdcdc;
      }
    ANGULAR: ANGULAR1
